# Local development. Copy to .env and adjust as needed.
# Matches docker-compose.yml (postgres:16, user/pass/db = prm).
DATABASE_URL="postgresql://prm:prm@localhost:5432/prm?schema=public"

# Next.js
NEXT_PUBLIC_APP_NAME="Sreedhareeyam PRM"

# --- Phase 3+ integrations (leave blank for local dev) ---
WHATSAPP_API_TOKEN=""
SMS_GATEWAY_KEY=""
EMAIL_API_KEY=""
